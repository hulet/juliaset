= Julia set real-time rendering demo

An interactive Julia set fractal rendered in WebGL.

Move the cursor (or drag on touch) to change the Julia constant. Click to freeze the view; scroll to zoom while frozen. Click again to return to live mode.

---

Forked from https://github.com/monman53/juliaset[monman53/juliaset].
